import React from "react"
import Layout from "../components/layout"

const Daily = ({ location }) => (
  <Layout location={location}>
    <div>
      <h1>Daily JS snippets go here</h1>
    </div>
  </Layout>
)

export default Daily
